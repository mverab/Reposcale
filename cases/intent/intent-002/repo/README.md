# Internal API

REST API for internal tools and mobile clients.

## Auth
Uses JWT authentication. See `app/auth_jwt.py`.

> **Note:** Some legacy session code exists in `auth_session.py` — migration in progress.
